<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <ImageView
        android:id="@+id/worker_image"
        android:layout_width="80dp"
        android:layout_height="80dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_marginLeft="10dp"/>

    <TextView
        android:id="@+id/worker_choice"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toRightOf="@id/worker_image"
        app:layout_constraintTop_toTopOf="@id/worker_image"
        app:layout_constraintBottom_toBottomOf="@id/worker_image"
        android:layout_marginStart="20dp"
        android:textColor="@color/black"
        tools:text="Scarlett hasn't decided yet"/>

    <view
        android:layout_width="0dp"
        android:layout_height="0.5dp"
        app:layout_constraintTop_toBottomOf="@id/worker_image"
        app:layout_constraintBottom_toBottomOf="@id/worker_image"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="@id/worker_choice"
        android:layout_marginEnd="1dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>